// using our own module which i created named as randomInteger.js

let randomInt = require("./randomInteger");//"./ "=> this is used to specify the path.

console.log(randomInt());
console.log(randomInt());
console.log(randomInt());